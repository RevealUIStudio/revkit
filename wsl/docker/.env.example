# Forge infrastructure — copy to .env and set real values before starting services
# Generate a strong password: openssl rand -hex 16
POSTGRES_USER=forge
POSTGRES_PASSWORD=change-me-before-use
POSTGRES_DB=forge
FORGE_DATA=/mnt/forge/databases
