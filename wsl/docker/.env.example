# Studio infrastructure — copy to .env and set real values before starting services
# Generate a strong password: openssl rand -hex 16
POSTGRES_USER=studio
POSTGRES_PASSWORD=change-me-before-use
POSTGRES_DB=studio
STUDIO_DATA=/mnt/studio/databases
