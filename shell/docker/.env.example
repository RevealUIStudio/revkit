# Sandbox infrastructure — copy to .env and set real values before starting services
# Generate a strong password: openssl rand -hex 16
POSTGRES_USER=sandbox
POSTGRES_PASSWORD=change-me-before-use
POSTGRES_DB=sandbox
SANDBOX_DATA=/mnt/sandbox/databases
